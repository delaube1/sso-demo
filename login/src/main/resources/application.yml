# 应用服务 WEB 访问端口
server:
  port: 9000
spring:
  datasource:
    url: jdbc:mysql://ali.kiroe.cn:3306/sso
    username: root
    password: jiejie123.
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapper/*.xml
  #目的是为了省略resultType里的代码量
  type-aliases-package: com.example.login.pojo